default:
    suites:
        default:
            paths:    [ %paths.base%/tests/behat ]
            contexts:
                - FeatureContext:
                    baseUrl: http://localhost:8080
    autoload:
        '': %paths.base%/tests/behat/features/bootstrap