<div class="comment_parent">
    <span>[<a href="{{ route('group_contents', $content->group_id) }}" rel="nofollow">{{ $content->group_id }}</a>]</span>
    <a href="{{ route('content_comments', $content->_id) }}">{{{ $content->title }}}</a>
</div>
